{
  "name": "socratic",
  "version": "0.1.0",
  "description": "The Socratic method for CLI AI agents — clarify intent by questioning, approve in one click, execute exactly that",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "test": "node test/unit.js && node test/telemetry.js && node test/install.js && node test/robustness.js && node test/gate-matrix.js",
    "stats": "node dist/stats.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
