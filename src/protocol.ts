/**
 * 주입 프로토콜 본문 (D9~D14, docs/research/question-design-ux.md §7 기반).
 * 매 실질 프롬프트마다 additionalContext로 주입되므로 간결함이 중요하다.
 */
export const PROTOCOL = `[mind-reader 프로토콜 — 시스템이 강제하는 의도 파악 절차]
사용자의 이번 요청을 실행하기 전에 아래 절차를 따르라. 승인 전에는 파일 수정·명령 실행이 시스템에 의해 차단되어 있다(조사용 읽기는 허용).

0) 이 입력이 진행 중 문답의 답변이거나 자명한 후속 지시면 절차를 재시작하지 말고 흐름을 이어가라.
1) 탐색 우선: 코드·파일·문서를 읽어 해소되는 불확실성은 사용자에게 묻지 말고 직접 조사하라. 질문은 사용자만 아는 것에만 쓴다.
2) 발산 측정: 이 요청의 실행 계획을 서로 다르게 3~5개 상상하라. 관측 가능한 결과(산출물의 형태, 변경되는 파일, 부수효과)가 갈리는 축을 찾아라. 갈리지 않으면 질문하지 말고 6)으로 — 질문 0개는 정상 결과다.
3) 가정 강등: 갈리는 축 중 이 저장소의 기존 패턴이나 표준 관례로 답이 정해지는 것, 틀려도 고치기 싼 것은 묻지 말고 '가정'으로 기록하라.
4) 질문 루프: 남은 축을 (결과를 가르는 정도 × 오답 시 되돌리기 비용)순으로 정렬해 가장 크게 가르는 것부터, 한 번에 하나씩 물어라. AskUserQuestion 도구를 사용하고(선택지 2~4개, 권장안 표시, 직접 입력 가능), 질문마다 이 답이 무엇을 가르는지 한 줄로 밝혀라. 사용자가 표현을 어려워하면(모호한 형용사 반복, "뭐라 해야 하지", 연속 "둘 다 아님") 각도를 바꿔라: 선택지 → 내 추측안을 구체적으로 제시하고 틀린 부분만 교정받기 → 초소형 예시·시안을 만들어 보여주고 반응 받기.
5) 질문 개수 제한은 없다. 의도가 정확히 파악될 때까지 계속하라. 단 모든 질문은 답에 따라 계획이 실제로 달라지는 질문이어야 한다. 파악됐다고 스스로 판단해도 종료 판정은 사용자만 한다 — 반드시 6)으로. "그냥 알아서 해"라는 신호가 와도 결과를 가장 크게 가르는 미해결 축 1~2개만 압축 확인하고 나머지는 가정으로 처리하라.
6) 의도 확인·승인: 승인은 반드시 AskUserQuestion 선택창으로 받아라. 채팅에 '승인'을 입력하게 하지 마라.
   · 발산 없음(질문 0개): 왕복 한 번으로 끝내라 — AskUserQuestion 하나를 호출하되, question 텍스트를 정확히 [MR-SPEC] 로 시작해 "이렇게 이해했다"를 한 문단으로 재진술하고(변경·생성될 파일과 비가역 작업 여부 포함), 선택지는 "승인"(정확히 이 라벨)과 "수정 필요"로 하라. 사용자가 이미 명시한 내용을 되묻지 마라 — 재진술과 선택이 전부다.
   · 발산 있음: 질문 루프를 마친 뒤 첫 줄이 [MR-SPEC] 인 한 화면 분량의 명세를 본문으로 제시하라. 구성: ✓ 사용자가 답한 것 / ~ 내가 가정한 것(항목별 개별 확인 표시) / 하지 않을 것(범위 밖 명시) / 변경·생성될 것(파일·산출물) / 검증 방법. 그리고 같은 턴에 AskUserQuestion으로 "이 명세대로 진행할까요?"를 물어라 — 선택지 "승인"과 "수정 필요". 명세 전문이 화면에 보이는 상태에서 선택하게 하라.
7) 사용자가 '승인'을 선택하면 시스템이 차단을 해제한다(채팅에 '승인'을 직접 입력해도 동일하게 인정된다). '수정 필요'나 직접 입력 답변이 오면 그 내용을 반영해 6)을 다시 밟아라. 비가역 작업(삭제·덮어쓰기·배포·force push·외부 전송)은 "알아서 해" 신호가 있어도 반드시 6)의 확인을 거쳐라. 실행은 대화 이력이 아니라 확정된 명세를 기준으로 하라.`;

/** 승인 직후 주입되는 안내 */
export const APPROVED_NOTICE = `[mind-reader] 사용자가 방금 의도 명세를 승인했다. 쓰기 차단이 해제되었다. 확정된 명세([MR-SPEC])를 기준으로 실행하라. 명세에 없는 작업을 추가하지 마라.`;
