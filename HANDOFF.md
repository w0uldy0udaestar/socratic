# HANDOFF — socratic (구 mind-reader)

> 최종 갱신: 2026-08-12 | 브랜치 `feature/m4-intent-discovery` (origin에 push 완료, main 미머지)

## 현재 상태 요약

socratic은 CLI AI 에이전트가 모호한 요청을 추측으로 처리하지 못하게 막고, 선택지 질문으로 의도를 파악한 뒤 명세를 승인받아야 실행하게 만드는 훅 기반 도구다. M3 재설치 dogfooding에서 승인 채널 마찰은 해소됐으나 **"이게 맞냐 확인하는 정도의 역할"로 퇴화**한 것이 드러났고(사용자 교정: "아키네이터처럼 질문을 계속해서 내 의도와 생각을 알아내야 한다"), 이에 따라 **M4 의도 발견 프로토콜 개정(D17)을 구현 완료**한 상태다(테스트 201건 전부 통과). 남은 것은 재설치 후 개정 프로토콜 dogfooding.

**M4 핵심 (`docs/m4-intent-discovery-notes.md`)**: ① 발산 측정 대상을 '실행 계획'에서 **'결과물 그림'(목적·형태·기준·취향·제약)**으로 전환 — 실행이 자명해도 그림이 갈리면 모호한 것 ② **모호도 비례** — 명시된 것은 되묻지 않되(D16 유지), 압축 확인 직행은 완전 자명한 지시만 ③ **확신 수렴** — 라운드마다 "'정확히 그거'라고 할 확신"을 재평가, 확신 전 명세 제시 금지 ④ "질문 0개는 정상 결과" 문구 삭제. 훅 코드·승인 마커([MR-SPEC], '승인' 라벨)는 불변이라 훅 3종은 손대지 않았다.

> **현재 이 컴퓨터에 설치돼 있지 않다** (2026-08-10, 개정 전 설치본 제거). `./uninstall.sh`로 훅 3종만 제거했고 `~/.mind-reader/`의 명세 아카이브 98건·지표 이벤트 329건은 보존했다 — M4 검증의 M3 기준선이다. 제거 직전 백업은 `~/.claude/settings.json.mind-reader.bak-1786343699570`.

## 완료·미완료

- [x] M0 실측 스파이크 — 훅 3계층(주입·게이트·Stop) 실기기 검증
- [x] M1 코어 — 훅 3종 + Bash 가드 + 프로토콜 + 설치 스크립트, 코드 리뷰 critical 4·major 8 수정
- [x] M2 계측 계층 — 이벤트 로깅 + `npm run stats` 집계 CLI
- [x] M2 실제 설치 — 전역 설치 완료, 기존 사용자 훅 11건 보존 확인
- [x] M2 결함 수정 5건 — idle 데드락 / uninstall 식별 실패 / 프로젝트 예외 부재 / 셀프테스트 무력화 / **승인 데드락**
- [x] 전체 QA — 견고성·게이트 매트릭스·E2E 179건 통과, 경로 정규화 결함 1건 수정 (`docs/qa-report.md`)
- [x] M2 dogfooding — 설치·실사용·제거까지 완주. 결론은 "마찰 과다"
- [x] **M3 마찰 완화 구현** — 승인 채널 선택창 전환(D15) + 발산 0 압축 확인(D16), 신규 테스트 21건 포함 전체 통과
- [x] M3 코드 리뷰 — major 5건(모양 위조·타임스탬프 fail-open·무관 질문 승인·조건부 승인·거부 명세 바인딩) 전부 수정, 상세 `docs/m3-friction-notes.md` §코드 리뷰 반영
- [x] M3 검증: 재설치 dogfooding — 승인 마찰은 해소, 그러나 확인 게이트 퇴화 발견 → M4 (2026-08-10)
- [x] 리브랜딩 — 레포·README·package.json을 socratic으로, public 공개 (내부 식별자 리네임은 IDEAS 백로그)
- [x] **M4 의도 발견 프로토콜 개정(D17)** — 그림 발산 측정·모호도 비례·확신 수렴, 테스트 201건 전부 통과
- [x] M4 프롬프트 리뷰 — 탈출구 상 3·중 2건 반영(가정 강등 남용·강등 은닉·0항 훅 모순 등), `docs/m4-intent-discovery-notes.md` §프롬프트 리뷰 반영
- [x] 개정 전 설치본 제거 — 훅 3종만 제거, 명세·지표 보존 (2026-08-10)
- [ ] M4 검증: 개정 프로토콜 재설치 dogfooding — 질문 라운드 수 분포·명세 수정률·질문 품질 (`docs/m4-intent-discovery-notes.md` §검증 계획)
- [ ] M4 2단계 판정 — 텍스트 개정으로 부족하면 훅 강제(질문 0회 명세 차단) 추가
- [ ] v0.2 Gemini CLI 어댑터 (`BeforeAgent`/`BeforeTool`/`ask_user`)

## 다음 단계 + 재개 방법

**1순위: 개정 프로토콜 재설치 dogfooding.** 현재 미설치 상태이고 M4 개정은 소스에만 있다. 재개 절차는 정확히 이 순서다:

```bash
cd /Users/universe/orca/workspaces/mind-reader/anglerfish
npm test              # 201건 통과 확인
npm run build         # ★ 필수 — 훅은 dist/를 직접 가리킨다
./install.sh          # 셀프테스트 통과 확인
grep -c 아키네이터 dist/protocol.js   # 1 이상이어야 M4 반영본
```

그 뒤 일상 작업에 투입해 관찰한다: ① 모호한 위임형 요청에서 질문 라운드가 실제로 발생하는가(0라운드 지배적 = 개정 실패) ② 자명한 지시는 여전히 선택창 1번으로 끝나는가(D16 유지 확인) ③ 명세 수정률(0에 가까우면 확인 도장화 신호) ④ 답을 이미 아는 질문이 나오는가. 지표는 `npm run stats` — 기존 329건이 M3 기준선이므로 재설치 시점을 기록해두고 전후를 나눠 본다. 부족하면 2단계 훅 강제로.

**먼저 열어볼 파일**: `docs/m4-intent-discovery-notes.md`(퇴화 진단·개정 내역·프롬프트 리뷰·검증 계획) → `src/protocol.ts`(개정 본문) → `PLAN.md`(D17·M4).

**머지 판단**: `feature/m4-intent-discovery`는 origin에 push돼 있고 main과 충돌 없다(main은 이 브랜치의 조상). dogfooding으로 개정 효과를 확인한 뒤 머지하는 편이 롤백이 쉽다.

## 주요 결정사항과 이유

- **훅 기반**(D4) — "상시 자동"을 결정론적으로 지원하는 유일한 수단. 스킬·지침 파일은 모델 재량이라 강제력이 없다.
- **2단 강제**(D6) — 프로토콜 주입(soft) + 승인 전 쓰기 차단(hard). 주입만으로는 모델이 무시할 수 있다.
- **종료는 수렴 단일**(D9) — 질문 개수 제한 없음. 사용자의 1순위가 "얼렁뚱땅 넘어가지 않는 것"이다. 피로 관리는 개수가 아니라 질문 가치 밀도로 한다.
- **모든 작업 유형이 1급**(D11) — 코딩뿐 아니라 글쓰기·리서치도 대상. 비파일 작업은 Stop 훅으로 게이트한다.
- **명시적 승인은 항상 존중** — 명세 마커를 못 찾아도 사용자가 "승인"하면 해제한다. 마커를 요구하면 데드락이 생기고, 이는 "종료 판정자는 사용자"(D9)에 반한다.
- **승인 채널은 선택창(D15)** — 사용자가 설계한 "선택지 질문"은 처음부터 AskUserQuestion UI를 의미했다. 텍스트 승인 요구는 M2 제거의 직접 원인. 훅 3종이 모두 `resolveAskApproval`을 거치는 이유는 "한 훅만 보다가 생기는 데드락"을 구조적으로 없애기 위해서다.
- **명시적 지시에 되묻지 않는다(D16)** — 압축 확인은 재진술 + 선택 1번이 전부. 이미 명시된 내용을 다시 질문하는 것은 도구의 목적(의도 파악) 전도다.
- **질문은 실행 리스크 완화가 아니라 의도 발견(D17)** — 아키네이터처럼 결과물 그림을 좁혀간다. M2 마찰의 원인은 승인 채널이었지 질문이 아니다. "질문 0개는 정상 결과" 같은 질문 억제 문구가 확인 게이트 퇴화의 원인이었다. 강제는 프로토콜 개정 먼저, dogfooding으로 부족하면 훅 강제 2단계.
- **상태는 프로젝트 밖**(`~/.mind-reader/`) — 게이트 대상인 Bash가 상태를 덮어써 스스로 승인하는 것을 막는다.
- **승인 후엔 무의견 통과** — `allow`를 반환하면 사용자의 기존 권한 설정까지 우회해버린다.

## 주의사항·리스크

- **재설치 전에 반드시 `npm run build`.** 설치된 훅은 이 worktree의 `dist/hooks/*.js`를 절대경로로 직접 가리킨다(`~/.mind-reader/install.json` 참조). 빌드를 빼먹으면 개정 전 프로토콜이 그대로 돌고, worktree를 옮기거나 지우면 훅이 깨진다. 막힐 때 탈출구는: 그 폴더에 빈 파일 `.mind-reader-off` 생성(프로젝트만) / `MIND_READER_OFF=1`(전역 즉시) / `./uninstall.sh`(훅 제거).
- **M4는 텍스트 개정만이다(1단계).** 모델이 프로토콜을 무시하고 여전히 확인 게이트로 동작할 수 있다 — 그래서 dogfooding 측정이 필수이고, 훅 강제(질문 0회 명세 차단)가 2단계로 예약돼 있다.
- **이 저장소에는 `.mind-reader-off`가 있다.** 자기 자신을 개발할 때 갇히지 않기 위한 것이니 지우지 말 것.
- ~~승인은 채팅에 "승인"이라고 직접 입력해야 한다~~ → **M3에서 해소.** 선택창 응답은 트랜스크립트 `toolUseResult.answers`에 구조화되어 남고 훅이 판독한다(실측 확인). "도구 결과라 읽지 못한다"는 M2의 전제가 틀렸던 것. 텍스트 '승인'은 백업 경로로 유지된다.
- **테스트는 `HOME`을 격리한다.** 이 격리를 풀면 `npm test`가 실사용 지표를 오염시킨다(임시 프로젝트 수십 곳이 집계에 섞였던 이력).
- **훅 프로세스 자체가 기동 실패하면**(dist 없음, node 없음, 타임아웃) 게이트가 열린다. Claude Code가 non-blocking 오류로 처리하기 때문이며, 코드로는 막을 수 없다. `install.sh`의 셀프테스트가 유일한 방어선이다.
- **Stop 훅 트랜스크립트 레이스** — 명세를 제시했는데 기록 지연으로 감지되지 않아 종료가 불필요하게 한 번 차단되는 경우가 있다. 안전 방향 오류라 우선순위는 낮다.
- **서브에이전트 우회 없음 (2026-08-04 QA에서 실증)** — 서브에이전트 내부 도구 호출도 `PreToolUse`를 거치고 부모와 같은 `session_id`를 쓴다. `Task`/`Agent` 허용은 안전하다.
- **미검증**: 세션 병렬 사용 시 승인 격리.
- **선택창 승인의 도구 바인딩은 모양 검증까지만** — 사이드체인·배열·빈 questions는 배제했지만, tool_use_id를 역추적해 실제 AskUserQuestion 호출에 바인딩하는 완전한 검증은 후속 과제. 승인은 명세([MR-SPEC])가 있을 때만 성립하므로 실효 위험은 낮다.
- **ask 승인 경로에는 APPROVED_NOTICE 주입이 없다** — PreToolUse/Stop은 주입 채널이 없는 구조적 한계. "명세 기준 실행" 규율은 프로토콜 7항 텍스트에 의존한다.
- **QA 관찰 4건** — 자명한 요청에도 명세·승인 강요(우선순위 높음), Stop 3회 차단 소진, HOME 쓰기 불가 시 게이트 무력화, 예외 파일 탐색 12단계 한계. 상세는 `docs/qa-report.md`.
- 되돌리기 어려운 git 작업(force push, reset --hard, 브랜치 삭제)은 사전 확인 필요.

## 저장소

- 로컬 정본: `/Users/universe/Project/mind-reader` (작업은 orca worktree `anglerfish`에서. main은 정본에 체크아웃돼 있어 worktree에서는 feature 브랜치로 작업)
- 원격: https://github.com/w0uldy0udaestar/socratic (public, 2026-08-04 리네임·공개)
